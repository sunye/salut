image: maven:latest

variables:
  MAVEN_OPTS: "-Dhttp.proxyHost=proxy.ensinfo.sciences.univ-nantes.prive -Dhttp.proxyPort=3128"

cache:
  paths:
    - .m2/repository/
    - target/

build:
  stage: build
  script:
    - 'mvn  compile'

test:
  stage: test
  script:
    - 'mvn test'

deploy:
  stage: deploy
  script:
    - 'mvn deploy'
  only:
    - master